# 作業開始時の指示

最新（2026-09-13）：デモは眉/目線/まぶた/口の強調を全1に固定。UIは4独立0〜1スライダー、普通のアバターでは0が追加作用なし、保存demoを選ぶと全1固定。demoの口寄せ/目線不足TCキーは実行時補完、既存キーと保存原本は維持。旧「デモで生成は眉のみ」「shiftL未マップ」は今回変更。目閉じの検出は変更せず表示感度のみ追加。docs/FACIAL_EXAGGERATION.md。

最新（2026-09-13）：眉にも可変追従を既定ONで追加。4フレーム後の目標と表示値の差0〜0.3でrate6〜45へsmoothstep、左右内外4値独立、欠測保持。--no-adaptive-brow-followで旧直接反映。頭の可変追従は別設定で未採用のまま。目閉じは変更しない。

最新（2026-09-13）：眉左右独立を実装。デモ/auto-customは実行時コピーへTC_BrowLeft/Right Up/Down/Sad/Angryを生成（眼骨基準）、既存キーの左右別を優先。existingは生成せず既存左右キーを探索、共通のみなら共通表示。保存デモ原本は維持。旧「デモは一切生成しない」は眉について本指定で変更。目の閉じは引き続き案のみ。

最新（2026-09-13）：今後の検証はデモ用シェイプキーアバターを指定。最新Player＋builds/demos/haolan-custom-brows/avatars/haolan.tcap＋--use-demo-shape-keys（既存TCキーを使用し生成しない）。保存済みデモPlayer/素材は上書きしない。通常testは-DemoAvatar。眉gain2を実装、1で旧感度。目の閉じ改善は今回案のみで、勝手に実装しない。

最新（2026-09-13）：bat整理。desktop直下はtanakacap.bat/test/face-capture/live/motionの5本のみ。比較・個別試行・デモの補助batはdesktop/tanakacap-toolsへ。tools/update-desktop-launcher.ps1がこの配置を維持する。補助batを直下へ再増殖させない。過去記述の補助bat配置は本項を優先。

最新確定（2026-09-13）：ユーザー指定で頭角度の既定はpnpへ復帰。size2dは任意試行。旧size2d既定記述より本項を優先。頭の可変追従は別の未採用試行で、通常は固定速度。

最新試行（2026-09-13）：頭角度size2dを可逆既定へ。眼間距離と鼻位置のXY比率でpitch/yaw、眼軸でroll、口・眉の既存PnP補正は維持。UI頭角度/pnpまたは-HeadPoseMode pnpで復帰。頭のみ専用モデルは対象外。既存録画の数値だけではpitchのガタつき改善は確認できず、採用品質は未確定。docs/HEAD_SIZE_TRIAL.md/HANDOFF先頭。旧通常pnp記述より本項を優先。

最新（2026-09-13）：操作UIを実装。desktop tanakacap.bat / run-ui.ps1、capture_lab/control_panel.py。開くだけではカメラ起動なし。実写表示なし、UIから許可フラグを渡さない。設定はui-settings.json（Git除外）、保存して適用は再起動。3推論モード、部位OFF、推論Hz/描画fps/受信Hz、60/同期/30/自由fpsと推論開始前の上限制御、解像度、AA、表情を実装済み。docs/CONTROL_PANEL.md。倍率は頭のみ=1の推論ms/観測、測定済み約2/2/1、個別組み合わせは未測定と表示。旧「UI未実装」より本項優先。RTMW-LだけFP16 Graph非互換のためFP32 CUDA Graphへ明示固定、全部ON RTMW3DはFP16維持。

最優先の表示ルール（2026-09-13）：実写のカメラ映像（録画由来/切出し/診断重畳も含む）は、Python起動時に完全一致の --explicitly-allow-displaying-raw-camera-images-on-screen-for-this-session-only がある場合以外、画面へ出さない。通常bat/診断/頭ROI選択/比較撮影/将来UIも共通。短縮形・旧--preview・JSON・環境変数・記憶したUI状態・ホットキーで許可を代替しない。長い引数を標準ランチャーに自動追加しない。実写表示はcapture_lab/camera_display.pyのガードを通す。別GUI/ブラウザ/ツール画像表示への迂回を作らない。tests/test_camera_display.pyを維持し、新しい表示API/入口を追加したら検査範囲も拡張する。アバター表示/OBS出力や録画保存とは別の要件。実カメラ試験の事前相談ルールも引き続き有効。

最新（2026-09-13）：通常既存キー版も口角のガンマ/開口時上げ抑制/強調を調整可能、未設定は1/0/0（追加作用なし）。自動独自キーは2/.9/0を維持。JSON/起動指定と将来UI接続を用意。Perfect Sync/ARKitのmouthLeft/Rightと左右smile/frownがあれば通常版も横寄せ/非対称を駆動する。HAOLAN通常版の不足と機能全体の未実装を混同しない。

最新（2026-09-13）：眉を実装し旧表情のデモをbuilds/demos/haolan-custom-browsへ保存（ソース6806913）、上書きしない。通常はexpression-mode existing：機能別ARKit/Perfect Sync→MMD→VRC、目線を含め独自キーを生成しない。auto-customは任意の実験方式で、過去の左右口角・唇限定横寄せ・瞳限定移動を踏襲。眉専用比較動画は不要。Perfect Sync推論の再実装ではない。docs/EXPRESSION_PORTABILITY.mdとHANDOFF先頭を参照。

最新指定（2026-09-13）：Fを通常ONへ採用。ユーザーがCUDA FP16を通常採用。FP32の起動引数/設定キーを削除し内部APIのみ保持する。将来UIへの追加は可能性のみで確定要件ではない。TensorRTは契約条件の説明後、ユーザーが「使わない」と明示し不採用。接続/追加依存を削除。再開を提案せず、docs/PRECISION_COMPARISON.mdとHANDOFF先頭を参照。A一括保留の旧記述より優先。E/揺れ物は触らない。

最新（2026-09-13）：Fの通常OFFはユーザー指定ではなく初回b3d583fでのエージェント判断だったと履歴確認。FをOFFにするユーザー発言があったと扱わない。ユーザー指定でF-OFF/ON全編動画比較を追加、docs/FURTHER_OPTIMIZATION.mdとHANDOFF先頭参照。

最新（2026-09-13）：口輪郭の推定Zを外す試行を通常pnpへ設定。固定テンプレート/PnPによる顔向き補正は維持、直前pnp_depthmouthへ可逆。3平均/stride1、口角追加強調0。動画はresults/avatar-videos/mouth-no-z、見た目はユーザー評価。docs/MOUTH_NO_Z_TRIAL.md。配布棚卸しはdocs/DISTRIBUTION_LICENSES.md。TensorRTは今回見送り、通常版も再配布自体は可能、RTX版特有の性能開示制限と区別し既存CUDAにも同種の配布義務があることを隠さない。実装・性能検証済みと扱わない。

最新結果（2026-09-13）：口角強調度0〜1連続、既定0で追加増幅なし、1で従来へ。将来UIに追加。J/K/Lは全てFull HD60＋OBSで比較しrevert済み。Jは同等で利点確認できず、Kは速いが推定差大、Lは僅かに遅い。G+Iを維持し試行フラグは残さない。最終228 tests。docs/MOUTH_EMPHASIS_AND_OPTIMIZATION_JKL.mdとHANDOFF先頭。

最新UI要件（2026-09-13）：推論切替UIに実測の推論速度を表示する。描画fps/Player受信Hzと混同しない。UI実装は将来。docs/SHARED_PREVIEW.md参照。

最新指定（2026-09-13）：描画共有・自由解像度・プレビュー非表示の3案を実装。既定Full HD。将来の推論切替UIに60/推論同期/30/自由入力を置き、数値上限より速い推論は計算開始前に抑える予定。UI/推論上限は今回未実装。最新検証はHANDOFFとdocs/SHARED_PREVIEW.md。揺れ物調整・A/E保留を維持。

最新比較（2026-09-13）：描画30対60を既存録画＋OBS60で評価済み。通常60維持、-RenderFps 30とdesktop test-30fpsで任意比較。受信44.47→46.14Hz、GPU使用率53.0→47.7%（短期参考値）。追加案の出力テクスチャ再利用等は未実装。docs/RENDER_RATE_COMPARISON.md参照。揺れ物コードは変更しない。

最新高速化（2026-09-13）：Fは実装済み任意（既定OFF）、G=crop前処理とI=batch_eyesを通常採用。Hは53772c7で実装、計測で遅くcee5557でrevert。顔はbody3d/pnp_depthmouthのまま。既存録画＋Player/OBS60秒比較39.317→44.833Hz・描画約60fps/透過成功。カメラfpsや実遅延ではない。docs/FURTHER_OPTIMIZATION.md参照。

最新指定（2026-09-13）：全部ONのF〜I高速化を実装。Hは独立コミット、遅ければrevert。Fは分割Graph実装/録画一致/GPU/900観測比較済み、効果小で任意。G→H→Iが進行中。docs/FURTHER_OPTIMIZATION.md参照。

最新採用（2026-09-13）：ユーザー評価で顔Zピッチを撤回、口角Zは採用。通常face_source=body3d/head_pose_mode=pnp_depthmouth。ピッチだけPnP、口角はZ。depth3dは再現専用。HANDOFF先頭参照。


最新試行（2026-09-13）：head_pose_mode=depth3dでRTMW3D顔Zからピッチ/口角を同時計算、pnpで復帰。face_source=body3d必須、通常separate/pnpを維持。今回の動画の目視確認はユーザーが担当、エージェントは映像抽出/目視をせず機械検証のみ。docs/FACE_DEPTH_TRIAL.md参照。

最新試行（2026-09-13）：RTMW3D-Xの顔XY共有をユーザー指定で可逆実装。face_source=separateが通常、body3dは体と1回の推論を共有。補正と虹彩モデルは固定し、既存録画で比較する。test.batは共有、test-face-original.batで従来へ。docs/FACE_SOURCE_TRIAL.mdとHANDOFF先頭参照。見た目評価前に通常採用へ変更しない。

最新検証方針（2026-09-13）：検証は既存録画を基本とする。ユーザーが明示的に指示するまで、エージェントは実カメラを開いて試験しない。録画では確認できず実カメラが不可欠と考える場合は、目的・録画で代替できない理由を説明して事前に相談する。一般的な自走許可を実カメラ試験の許可と解釈しない。ユーザー自身が起動するカメラ用batは維持する。

最新指定（2026-09-13）：高速化は全部ONを対象にB（GPU転送・起動）→C（人物ROI追跡）→D（人物検出小型化）の順に実装・評価する。A（TensorRT/FP16）とE（部位更新頻度）は保留。手を止める必要がある胴体推論OFF案は採用しない。揺れ物はスポーン時を含め今後調整しない。音声口パクは後日。結果はdocs/FULL_MODE_OPTIMIZATION.md、最新状態はHANDOFF先頭。

このプロジェクトの会話履歴は、次回のセッションに存在するとは限らない。作業前にこのファイル、`HANDOFF.md`、`docs/SPEC.md`、`docs/PROGRESS.md`の末尾を読む。現在のユーザー指示を最優先する。

## プロジェクトの趣旨

- Unityに慣れたVRCアバターユーザーが、自分の改変済みアバターを少ない手間でWebカメラ用に持ち出せるソフトを作る。
- Unity Editor変換プラグインと、書き出したアバターを動かす専用アプリの二段構え。
- 着席、Webカメラ1台、リアルタイム。頭、目・口の開閉、上半身、大まかな手の動きが初期対象。
- シェーダーの見た目を重視する。髪や服は自然に揺れればよく、PhysBoneやVRChatの全ギミックとの完全互換は要求されていない。
- RTX限定でよい。ユーザー環境はRTX 4090。GPU推論を必須とし、まず4090で実用できる最高精度を追求する。軽量化を先行させない。
- 性能評価は専用アプリ＋OBSを基準とする。タルコフ等の重いゲームを同時に動かす利用意向はあるが、ユーザーは要件に入れないよう明示した。同時実行の性能保証・必須検証を追加しない。不可能と実証済みとも扱わない。
- MediaPipeへの依存は決定事項ではない。学習済みモデルの利用と、独自のGPU推論・追跡・補正・アバター駆動を有力な方向としている。
- 初期評価は申告カメラEES-CMS-V43BK-3と、無改変haolan（lilToon）。OBSでのキャプチャが主用途。コラボは相手の配信画面に映像として並ぶ用途で、共通3D空間は不要。送信ツールや透過方式は未決定で、提案を採用済みと扱わない。
- 初期の出力範囲は自分のOBSに映すところまで。Discord等でWebカメラとして選べる出力とコラボ映像送信は後日の追加要件。これらを初期の必須実装・検証に含めず、方式の未決定を理由に初期開発を止めない。

## 実装・技術選定で守ること

1. 確定要件と仮説を混同しない。CUDA、TensorRT、ONNX Runtime、個別の推定モデル、Unityバージョンは、検証前に採用済みと書かない。
2. 古い記事やサンプルに合わせて古い依存関係を無検討で導入しない。導入時点の公式資料、対応OS・GPU・Unity、配布条件を確認し、採用バージョンと理由を残す。「最新」という理由だけでも選ばない。
3. WindowsでCPU推論しかできない構成を主構成にしない。CPUへの暗黙のフォールバックをGPU対応と扱わない。画像取得や軽い制御までGPU化するという意味ではない。
4. OpenCVの有無・新旧だけで精度を説明しない。画像取得、モデル推定、時間方向の処理、アバターへの反映を切り分ける。前回の失敗した実装は未確認であり、原因を断定しない。
5. 学習済みモデルを使うことと、モデルをゼロから学習することを区別する。後者は現在の初期スコープではない。
6. 原本のVRCアバターや改変プロジェクトを壊さない。書き出し用コピーで変換し、未対応機能を黙って捨てない。依存ツールのビルド時処理も確認する。
7. ランドマークが表示されるだけで完成にしない。実際のアバターとカメラで、遅延、震え、遮蔽、復帰、着席時の安定性、描画との負荷競合を評価する。
8. GPU実行や高精度を未測定のまま断言しない。手元の観測、ユーザー申告、公式資料、提案を区別して記録する。
9. 普通のカメラで見えない関節や細かな表情の精度を保証しない。計算量の増加と実用品質の向上を同一視しない。
10. ユーザーは実装開始と6フェーズの文書化を承認し、実装完了まで通常の作業は再確認せず自走するよう指示した。重大な疑問のみ質問する。環境の権限制約は守る。アバター原本は既存の改変プロジェクトから流用せず、BOOTHから取得する指定になった。
11. 検証用コマンドをユーザーへ毎回入力させず、デスクトップのtanakacap-test.batを毎回更新してダブルクリック起動できる状態にする。tools/update-desktop-launcher.ps1を使用。手首調整後もユーザー確認待ちで止めず、独立して進められる次の作業を続ける。実人物で未検証の品質は合格と扱わない。
12. ユーザーは現在の追跡課題（点の振動、手首、短い欠測、奥行き、胴体ひねり）をひと通り実装するまで自走するよう指示した。小さな改善ごとに実使用確認を必須の停止条件にしない。実装済みと実用品質の確認済みは区別する。
13. 欠測保持時間を過ぎても、その他の追跡停止理由でも最後の姿勢で止める。待機姿勢へ戻さない。「追従が悪い」は応答が遅いという申告であり、可動域不足と読み替えない。指の姿勢取得も現在の実装対象へ追加された。

14. 最新の試行設定は3フレーム平均を1フレームずつずらし、隣接する平均2点（1〜3と2〜4、計4フレーム）で方向継続を確認する方式。頭・表情・胴体・腕・掌・指が対象。tracking-settings.jsonのobservation_block=3/observation_stride=1が最新、3/3が旧6フレーム、1/1が平均なしの従来2観測。可逆性を維持する。ユーザーは6フレーム方式の滑らかさ低下と、手首の異常回転の解消を確認した。手首の修正を壊さない。調査根拠はdocs/ARM_ROTATION_RESEARCH.mdに残す。

## 引き継ぎを維持するルール

- 最新（2026-09-13）：将来のUIで各オプション/構成の相対負荷を「約N倍」と整数表示するユーザー指定。基準と計測範囲はUI設計時に確定、現時点でUI実装は不要。docs/INFERENCE_BREAKDOWN.mdの測定条件とセットで扱う。

- 最新（2026-09-12）：最軽量目標は頭姿勢専用モデル＋音声口パクで、顔ランドマークも省略。音声口パクは将来課題として今回実装しない。通常は全モデルON、部位別OFFと時間内訳を評価。本家PhysBoneは開発Editor限定比較、SDKは配布物へ入れない。

- 最新（2026-09-12）：追加の軽量AAを既定ON、F7/--no-edge-aaで復帰。720p既定/1080p選択。フェーズ4の長時間/OBS/遅延はdocs/PHASE4_VALIDATION.md、各フェーズ未達はIMPLEMENTATION_PHASES最新表。描画約60fps・認識約20Hz、体感遅延/実人物品質の合格ではない。検証batはPlayer統計も保存し、live/motionの非記録無期限は維持。

- 最新（2026-09-12）：髪服は元PhysBone設定の互換変換を優先するユーザー指定。SDK本体は同梱せず独立近似。docs/SECONDARY_MOTION.md。本家一致は未確認。非記録/無期限のtanakacap-live.bat、自作モーションのtanakacap-motion.batを既存検証版とは別に維持。docs/LAUNCH_MODES.md。モーション専用0BSDで、アバター許諾とは別。

- 最新（2026-09-12）：フェーズ3のHAOLAN限定.tcap書出し/外部読込/隔離OBS実受信を検証済み。通常Playerは外部avatars/haolan.tcapが必要。AlphaOutput明示描画を維持。docs/PHASE3_PACKAGE_OBS.md。通常OBSシーンのユーザー確認、揺れ物・汎用改変対応・配布環境は残る。

- 最新（2026-09-12）：ユーザーが肩face_ratio/腕front_projectionを通常採用し次へ進むと指定。前項の通常legacy/採用保留は旧状態。品質残件は残してフェーズ3の最小書出し・外部読込・OBS実受信へ進む。docs/IMPLEMENTATION_PHASES.mdの最新監査参照。

- 最新（2026-09-12）：肩の前傾対策へモデル首/肩/胴体Zを使わないユーザー指定。顔肩距離比と支持最大幅を試行、肩基準短縮禁止。shoulder_yaw_mode=face_ratioで試行、legacyが通常既定。終盤静止ヨーが残り採用保留。専用tanakacap-test-shoulder.bat、docs/SHOULDER_FACE_RATIO_TRIAL.md参照。

- 2026-09-12ユーザー指定：実写録画はGitへコミットしない。実体はローカルに残して今後も検証に使う。results/の除外を維持し、動画をforce-addしない。履歴に混入していた場合は実体を保存したまま履歴からも除去する。

- 最新（2026-09-12）：前方専用の腕投影短縮復元を試行実装。arm_depth_mode=front_projection、legacyで復帰。手首のみ肩よりカメラ側、後方肘/負の前腕Zは禁止しない。校正長は下限、顔尺度不足は腕保持。docs/ARM_FRONT_PROJECTION_TRIAL.mdとHANDOFF先頭参照。実人物改善は未確認。

- 最新（2026-09-12）：ユーザーはSAM両モデルの肘、ViT-Hの肩/総合品質を評価。ViT-Hを実時間化候補の本命とする提案、配布条件調査はdocs/VITH_REALTIME_DISTRIBUTION.md。正式採用/30Hz達成/全依存の配布監査は未完了。Fast SAMの別GPU/DINO性能を本件の保証にしない。

- 最新（2026-09-12）：現行/SAM DINOv3/SAM ViT-H全編3方式動画4本完成。results/avatar-videos/body-three-models。顔等・補正固定、ライブ採用なし。CPU/GPU質問は単独profilerで細かいkernel起動と同期を確認、4090演算能力の限界と断定しない。docs/BODY_MODEL_COMPARISON.md、docs/SAM_PERFORMANCE.md。全編ジョブは終了、HANDOFF先頭優先。

- 最新（2026-09-12）：現行RTMW3D-X / SAM DINOv3 / SAM ViT-Hの動画比較をユーザー指定。接続と600frame結合検証、全基準再現成功。両モデル受領・CUDA検証済み、全編推論と自動動画化が進行中。docs/BODY_MODEL_COMPARISON.mdとHANDOFF先頭参照。

- 最新（2026-09-12）：現行対HaMeR指の実アバター動画3本完成。SAM承認・資産受領済み、生CUDA推論まで成功（単独中央値約716ms）。身体共通補正接続は未完了。docs/AVATAR_COMPARISON_VIDEOS.md、docs/SAM_INITIAL_VALIDATION.mdとHANDOFF先頭参照。

- 最新（2026-09-12）：SAM承認待ち。HaMeR全撮影CUDA推論と指だけの既存補正比較完了、ライブ接続は未実装。docs/HAMER_COMPARISON.md/HANDOFF先頭参照。既定モデル/顔/腕/掌は維持。有効率を正解率と扱わない。

- 最新（2026-09-12）：MANO_RIGHT.pkl受領、assets-source/manoへ配置。SAMは申請済み承認待ち。実撮影のfirst-take比較reportはcomplete、詳細監査未完了。HaMeR環境/3D接続は未完了。HANDOFF先頭参照。端末向け案内には生URLを添える。

- 最新（2026-09-12）：ユーザー撮影待ち。tanakacap-compare-capture.batで日本語ガイド・HuffYUV可逆録画、analyzeで共通入力/補正比較。実装済みは現行＋DWPose/RTMW-Xの2D対照。SAM/HaMeRは取得待ち・生出力ワーカー未検証・3D共通補正接続未完了。docs/COMPARISON_CAPTURE.mdを参照し全候補完成と扱わない。

- 最新（2026-09-12）：顔距離のガタつきへstableフィルター追加、legacyで復元。次は補正固定のモデル比較（docs/MODEL_COMPARISON_PLAN.md）。目線/口/掌は良好、胴体yaw/腕/左指/右手開閉時の肘が課題。OBSより距離・比較を優先。新モデル比較はまだ未実施。

- 最新（2026-09-12）：ピッチによる口角誤変形の解消をユーザーが確認。現在の頭/口補正を維持。次はフェーズ3のOBS実透過合成、最小Unity変換/ファイル読み込みへ。全追跡品質の合格とは扱わない。

- 最新（2026-09-12）：頭ピッチ改善はユーザー確認済み。口角の残る向き連動へmouth_lip_depth_scale=1.5を試行、1.0で前回補正。頭推定は維持。HANDOFF先頭参照。

- 最新（2026-09-12）：head_pose_mode=pnpで鼻/眼端の幾何ピッチと口角の正面座標補正。legacyで両方旧方式へ。頭yaw/roll、検出モデル、口角モーフは維持。起動安定10観測をpitch基準、gain1.8。docs/HEAD_PITCH_MOUTH.md。実人物精度未確認。

- 最新（2026-09-12）：gaze_gain=4（2で旧表示）、ロスト目線はカメラ方向へ緩やかに復帰。接近既定framedは軽い前傾＋全体移動で画面内の顔高さを維持、腰完全固定ではない。seatedで前回へ。「口角」は「広角」の訂正、口角は変更なし。docs/FRAMED_GAZE.md。実人物品質未確認。

- 2026-09-12以降はユーザー指示でローカルGitを使用し、適切な修正単位で検証後にコミットする。都度の許可は不要。原本アバター・モデル・結果・環境キャッシュは既存ignoreを維持。基準コミットa762348。外部pushの指示はない。

- 最新（2026-09-12）：接近の横飛びは前傾までyawへ回す合成順序を修正、着席のみRx(pitch)*Euler(0,yaw,roll)。前腕worldキャッシュを親回転差で移送、ArmHeldはローカル保持。実観測腕へ前傾を一律加算しない。最新接近536frame右腕欠測323、yaw最大64.45度急変は残る。docs/SEATED_COUPLING.md、実人物改善未確認。

- 最新（2026-09-12）：接近は着席前傾へ変更。face_distance_mode=seated（translateで旧全体移動）。腰/下半身/ルート固定、距離からSpine/Chestピッチ置換、頭はカメラ基準。旧比0.75制限は入力から撤去、代わりに後傾25/前傾65度の表示範囲。最新630/634有効、197frame旧接近限界でありロスト主因ではない。docs/SEATED_DISTANCE.md、実人物品質未確認。

- 最新（2026-09-12）：目は輪郭基準、旧左右反転の誤りを修正。gaze_reference=legacyで旧基準/反転へ。新要求の顔サイズでavatar全体前後移動を実装、face_distance_enabled=falseでOFF、ロストは最後の位置。顔/腕のモデル・校正は維持。docs/GAZE_CONTOUR_AND_DISTANCE.md、実人物改善未確認。

- 最新（2026-09-12）：ユーザー「目は少し動く」。212310記録の入力監査はdocs/GAZE_INPUT_AUDIT.md。610/737送信有効、平滑化前後の振幅はほぼ維持、左右眼一致とROI中央基準が次の検討候補。runtime/gain/model変更なし。正解視線がないため偏りを自動中立補正しない。顔/目の別系統は維持。

- 最新（2026-09-12）：再度目が動かない評価。顔は良好、顔/目は別系統を維持。最新471/555frame目線有効、実Player画像では移動あり。表示感度gaze_gain=2.0（1.0で前回）を試行、最大変形量は不変。実画像虹彩重心で左右端間5→10px確認。左右別検出診断を追加、全原因解決/実人物品質の証明とは扱わない。docs/GAZE_RENDER_AUDIT.md参照。

- 最新（2026-09-12）：目が動かない申告。389/453frameで目線送信有効、HAOLAN瞳の眼骨重み約3.5%が表示を弱めていた。既定gaze_render_mode=irisで瞳限定モーフ（横4/縦2.5mm）、bonesで旧表示へ戻せる。検出モデルは変更なし。精度最優先で選んだ根拠はなかった点と代替候補をdocs/GAZE_MODEL_REVIEW.mdに記録。実人物改善は未確認。

- 最新（2026-09-12）：追加要件の目線を可逆な試行として実装。gaze_enabled=falseで推論ごとOFF、F4は反映のみOFF。目線だけは欠測時に約1.5秒で95%正面復帰する明示的例外。ほかの欠測保持は維持。虹彩CUDA確認済み、実人物精度未確認。docs/GAZE_TRIAL.mdとHANDOFF先頭参照。

- 最新（2026-09-12）：口角は符号付きガンマ2、横寄せ最大4mm、唇限定を維持。OBSは透過指定、マゼンタ/クロマキー案を撤回。KlakSpout 2.0.6のRGBA送信を実装、実OBS受信は未確認。HANDOFF先頭を優先。

- 最新（2026-09-12）：口横寄せを10mmへ抑え、表示を腹部〜耳先へ寄せる。ユーザーは腕の暴れが残っても有効な追加案がなければ次へ進むことを希望。補正追加をひとまず止め、フェーズ3（書き出し/読み込み/OBS）へ進む。F3のOBS準備表示を追加。品質合格やOBS実取り込み完了とは扱わない。HANDOFF先頭参照。

- 最新（2026-09-12）：口横寄せで顎・首を動かす全renderer空間変形を撤去。作者「口_上」の対象頂点かつ唇中心上下14mm内のみ、左右別モーフで最大16mm。実Bakeで範囲外不変を検査する。外側後方の低い手には骨長/手首固定の後方肘prior。肩幅縮みだけのヨーは肩深度が正面相当なら抑制。過抑制を含む新しい見た目は未確認。HANDOFF先頭を優先。

- 最新（2026-09-12）：外側手のCrossBody微小値残留を修正し、外側で交差ゲートとfront時間保持を解除。顔相対肩幅は独立肩深度が正面で10安定観測の時だけ再取得。口横寄せは共通空間マスクで口内にも適用し最大16mmへ。舌制御は残存なし。頭内部の手首目標をカメラ側へ逃がす近似を追加。新しい実人物品質は未確認。HANDOFF先頭を優先。

- 最新（2026-09-12）：前回の後方肘の逆折れ解消はユーザー確認済み。への字口の誇張量は維持する。開口時は口角の上げだけ最大90%抑制、閉口横寄せmouthShiftを追加。反対側へ渡る手に限定したCrossBodyで前腕を胸郭基準の前へ逃がす。外側の手へ強制上腕反転を復活させない。新しい見た目は未確認。HANDOFF先頭を参照。

- 最新（2026-09-12）：左右非対称口はユーザー確認済み。左手を少し外へ広げ、左肘が後ろへ突き出す姿勢を正当なものとして保つ。外側手への広いfront判定と、手首前方から上腕を反転する処理を撤去。指の深い屈曲で軸が反転する処理を修正。HAOLANの通常口角へ下げ30%の試行補正を追加。人体品質は未確認。根拠/限界はdocs/ELBOW_ANATOMY_REVISION.mdとHANDOFF先頭。下記の旧上腕前方priorより本項を優先。

- 最新修正/指定（2026-09-12）：肩幅は顔サイズを基準とする相対値。ピクセル固定禁止。前回の最大幅更新が基準を0.341→0.407へ押し上げ正面ヨーを作る経路を確認し撤去、顔尺度補正後の初期安定比を使う。画面上の基準幅は顔サイズで毎回変わる。手は曲げを戻してから前腕ひねりを抽出し、曲げ上限を90度へ試行拡大。口角の差分感度/モーフ変形量を強化。詳細はHANDOFF先頭。

- 最新指定/修正（2026-09-12）：見えている手首が同側肩より内側（反対側を含む）なら必ず前方とする着席priorを追加。掌方向/高さによらない。画面外は従来通り保持。肩ヨーの基準幅は顔尺度補正した観測の5秒10支持最大へ変更し、固定0.36との不一致による正面ヨーを減らす。口角は左右上下実装済みで、初期の安定した口閉じ10観測を差し引く校正を追加。掌の退化時補助軸も追加。実品質は未評価、HANDOFF先頭を優先。

- 最新訂正（2026-09-12）：腕幅の検出率は全フレームではなく、画面内・信頼度十分・投影長十分な前腕を分母にする。輪郭不明/不整合は失敗に残し、画面外・低信頼・短縮は個別に報告。推定点での可視性であり目視正解とは区別する。画面を越える探索線の有効部分と、前腕の緩い先細りを測定できるよう改修。奥行き駆動は未接続。HANDOFF先頭を優先。

- 最新指定（2026-09-12）：肩ヨーの回転量は肩幅から、回転方向だけをひじ前後から得る。ひじ差の大きさを角度に使わない。顔尺度で肩幅を距離補正し、不良時はモデル肩深度由来の量へ代替。ひじ不良/深度差15mm未満は方向のみ保持。腕幅記録は左69/右136件（1522frames）と疎で、奥行き未接続。詳細はHANDOFF先頭。

- 最新試行（2026-09-12）：ユーザーは改善を確認し、腕の見かけの大きさと肩の震え対策を要求。画像から前腕の横断輪郭幅を診断記録する処理を追加したが、奥行きへは未接続。袖幅/回転/背景の曖昧さがあり、距離推定の成功と扱わない。肩/ひじ/腰のSimCCピークを局所補間する試行を追加（フレーム待ちなし）。大きなヨー飛びは別問題で未解決。詳細はHANDOFF先頭。

- 最新試行（2026-09-12）：ユーザーが袖の垂れをひじと誤認する現象を確認。経験的なヨー極性反転を撤去し、+Zが前の座標対応leftZ-rightZへ戻した。既存2D/3Dモデルの肩相対ひじXYが肩幅0.25倍超で不一致ならヨーのみ保持する試行を追加。前腕/手首は参照しない。袖の識別・深度の正解を得たとは扱わず、両モデル共通の誤検出は残る。HANDOFF先頭参照。

- 最新修正（2026-09-12）：腕・胴体の一斉停止は顔距離尺度の棄却が全身の無効化へ波及したもの。顔尺度が使えない間も有効な肩尺度で追跡し、腕長の学習と長さ由来の奥行き追加は休止（保存済み最大長は保持）。顔基準は単発ではなく10候補中8以上に支持される形から初期化。顔尺度不良だけを全身停止条件へ戻さない。詳細はHANDOFF先頭。

- 最新調整（2026-09-12）：ユーザーは右ひじ前進で左肩が出ると報告。ひじ由来ヨーの入力極性だけを反転した（腕・顔の左右/座標変換は変更しない）。掌の遠回り解消は確認されたが甲側へ届かないため、Unityの前腕駆動上限を±100→±160度へ拡大、手首60/±40と最短側選択は維持。人体の可動域測定値ではなく試行値。詳細はHANDOFF先頭。

- 最新指定（2026-09-12）：舌は実人物で不良のため削除済み。再追加しない。肩ヨーは左右のひじ深度差で決める（ひじが前なら同じ側の肩が前）。前腕/手首やそれを使う腕深度補正の出力をヨーへ使わない。肩幅由来ヨーは撤去。掌反転の遠回り再発に対し±140度の枝切替を撤去し、最短側を選んで制約端で停止する。HANDOFF先頭を優先。

- 最新指定（2026-09-12）：画面外のひじ部分追跡と、前回の手振り対策（指骨長履歴検査）はユーザー指示で撤去。再追加しない。首/胴体の左右を統一し、目/口角も解剖学的左右で検査する。口輪郭・舌を追加したが、舌は画像色/形の試験実装、ピッチは有効な腰観測がなく未解決。最新版はdocs/AXES_CONTOUR_TONGUE.mdとHANDOFF先頭。

- 最新指定（2026-09-12）：胴体の前後/ひねり制限を緩和。腕長は顔の相対サイズによる距離尺度で常時校正し、5秒10観測・短縮禁止を維持。右手回転再調整、手振り時の指点崩れ対策、口形状/強調を追加。最新の方法・未確認点はHANDOFF先頭とdocs/DISTANCE_AND_MOUTH_REVISION.mdを参照。古い「ForearmNeutral維持」等より最新版を優先する。

- 最新評価（2026-09-12）：手首の改善をユーザーが確認。画面端へ集まる手の点を制御へ使わないこと、伸ばした指の誤屈曲/震えの修正、口幅追跡を要求。画面外の減速案は必須ではなく、現在は最後の有効な関節姿勢の保持。胴体の不良は未解決で、補正追加より整理の優先を引き継ぐ。

- 最新優先（2026-09-12）：ユーザーは腕奥行きの改善を確認し、手首/胴体には「補正追加より整理」を要求。改善部分を保ち、推定・制御・ボーンの各段を同じ記録で比較する。腕固定の合成テストだけを実動作品質の根拠にしない。

- 2026-09-12の追加申告は「手首の制約がずれている/狭い」。従来の遅延の申告とは区別し、可動域の評価も行う。最新の手首・肩・奥行きの変更と検証はHANDOFF先頭を参照。数値制限を人体の普遍的な可動域と扱わない。

- 意味のある作業をしたセッションの終了時、および大きな判断・検証の後に、文書を更新する。
- `HANDOFF.md`は最新状態に書き換える。完了事項、進行中、未解決事項、次の具体的な作業、実行・検証方法があればそれを記す。
- `docs/PROGRESS.md`は日付付きで追記する。過去の判断を消さず、変更理由を新しいエントリに残す。古い内容が誤りなら訂正先を明示する。
- 要件変更は`docs/SPEC.md`にも反映する。仕様の状態と改訂履歴を更新し、ユーザー合意と提案を区別する。
- 完了と未検証を区別する。失敗や未実施の検証も記録する。数値は測定条件や結果ファイルへの参照と組み合わせる。
- 大きいログ、モデル重み、アバター原本、機密情報を進捗文書へ貼り付けない。必要な要約と保存場所を記す。
- 文書は日本語を基本とする。実ファイルと記述の整合性を確認する。

## 文書の役割

- `README.md`：プロジェクトの入口。セットアップ・ビルド・起動の現在手順を維持する。依存版、素材、引数、成果物、設定、OBS連携を変更したら同じ変更でREADMEも更新し、パス・引数・リンクを照合する。古い手順を追記で積み重ねず、履歴はPROGRESSへ移す。
- `docs/SPEC.md`：現在の要件と評価基準。
- `HANDOFF.md`：今この時点から再開するための情報。
- `docs/PROGRESS.md`：追記式の履歴。

文書に食い違いがあれば、現在のユーザー指示と最新の明示的な決定を優先し、必要な文書を修正する。
