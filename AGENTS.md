# 作業ルール

この文書には継続して守る方針だけを書く。実装履歴、測定値、設定値、完了報告を追記しない。

## 作業開始と文書の役割

- 現在のユーザー指示を最優先する。過去の提案を採用済みと扱わず、撤回済みの方式を勝手に復活させない。
- 作業前に [HANDOFF.md](HANDOFF.md) の最新状態、[SPEC](docs/SPEC.md) の関連要件、[PROGRESS](docs/PROGRESS.md) の末尾を確認する。会話履歴が次回も存在するとは限らない。
- `HANDOFF.md`：再開に必要な現在の状態、未解決・保留事項、次の作業と検証方法。最新状態に書き換える。
- `docs/SPEC.md` と機能別仕様書：現在の要件・決定・評価基準。要件を変えたときに更新する。
- `docs/PROGRESS.md`：日付付きの作業・検証・判断の履歴。過去の誤りは訂正先を示す。
- `README.md`：開発者向けセットアップ・ビルド・起動手順。関連変更と同時に更新する。
- `docs/USER_GUIDE.md`：利用者向けの簡潔な導入・操作説明。開発の技術情報を混ぜない。
- 同じ完了報告を各文書へ一律に複製しない。詳細は担当文書へ置き、他から参照する。日本語を基本とし、パス・引数・リンクを実物と照合する。

## 製品の目的と実装方針

- 改変済みVRCアバターを、Unity Editorの変換プラグインと専用アプリでWebカメラ用に持ち出す。着席・カメラ1台・リアルタイム・OBS出力を主用途とする。
- シェーダーの見た目を重視する。VRChatの全ギミックとの完全互換や、重いゲームとの同時実行保証は要求に加えない。
- Windows・RTX向けGPU推論を前提にする。CPUへの暗黙のフォールバックをGPU対応と扱わない。画像取得や軽い制御までGPU化する必要はない。
- 品質と応答性を優先し、性能改善は条件をそろえて比較する。モデル・依存ライブラリを導入／変更するときは公式の対応情報と配布条件を確認し、理由を残す。
- 既存の改善を保ち、推定・平滑化・送受信・アバター反映を切り分ける。特定アバターやカメラだけに合わせた対処で終わらせない。

## 原本・実写・カメラの保護

- アバター原本やVRC改変プロジェクトを破壊しない。ベイク・変換はコピーで行い、未対応機能や省略は警告とログに残す。
- 実写録画・モデル・アバター原本・結果・環境キャッシュ・機密情報・`memo.txt` はGitの既存除外を維持する。実写をforce-addしない。履歴へ混入した実写はローカル実体を保持して履歴からも除去する。
- 実写映像は録画由来・切り出し・診断重畳を含め、起動時に完全一致の `--explicitly-allow-displaying-raw-camera-images-on-screen-for-this-session-only` がある場合以外、画面に表示しない。
- 実写表示は `tanakacap/camera_display.py` のガードを通す。短縮引数・設定・環境変数・ホットキー・別GUIで迂回しない。通常ランチャーに許可引数を自動追加しない。新しい表示入口では `tests/test_camera_display.py` の検査も更新する。
- 検証は既存録画を基本とする。エージェントによる実カメラ試験は、ユーザーの明示的な指示がなければ目的と録画で代替できない理由を伝え、事前に相談する。一般的な自走許可は実カメラ試験の許可ではない。
- カメラ設定は利用者の選択を尊重し、機器を一意に特定できないとき変更しない。詳細は [CAMERA_COMPATIBILITY](docs/CAMERA_COMPATIBILITY.md) を確認する。

## 検証・成果物・作業の進め方

- 実装済み、人工入力／録画で検証済み、実人物で品質確認済みを区別する。測定条件・失敗・未検証も残す。有効率を正解率、他機種の資料や模擬試験を実機保証と扱わない。
- 承認済み範囲は通常作業ごとに確認を挟まず進める。重大な未決事項や権限制約は確認する。ユーザーの見た目確認待ちでも独立した作業は進められる。
- 検証方法を変えたら `tools/update-desktop-launcher.ps1` でデスクトップの `tanakacap-test.bat` を更新し、ユーザーがダブルクリックで確認できるようにする。現在の検証アバター・入力はHANDOFFを参照する。
- 一時検証ファイルはデスクトップの `tanakacap-tools`、継続利用する診断は `tools/diagnostics` へ置く。プロジェクト直下やデスクトップ直下へ検証用ファイルを増やさない。
- 適切な変更単位で検証後にローカルGitへコミットする。公開・ZIP作成は現在のユーザー指示に従う。別PCで試すために、変更後はコピーが必要なファイル／ディレクトリを具体的に案内する。
- 配布条件の根拠・監査は担当文書とビルドの検査を維持する。詳細なログや認証情報を進捗文書に貼らず、必要な要約と保存場所を記す。
