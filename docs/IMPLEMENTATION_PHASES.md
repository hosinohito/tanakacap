# 実装フェーズ

2026-09-11：会話で提示した6フェーズの案を、ユーザーがそのまま受け入れて文書化するよう指示した。各フェーズの状態はHANDOFF.mdと進捗ログで管理する。

| フェーズ | やること | 完了の目安 |
|---|---|---|
| 1. キャプチャ技術の比較 | 手元のカメラで顔・上半身の推定モデルをGPU実行して比較。映像に推定結果を重ね、速度・震え・取りこぼしを確認する | 採用候補と弱点を実測で説明できる。カメラ由来の問題も切り分けられる |
| 2. haolanを実際に動かす | 無改変haolanを開発用Unityプロジェクトで直接使い、頭・目・口・上半身・腕へ推定結果を反映する | 座って話す、手を振る動作が自然に伝わる。まだ汎用変換は不要 |
| 3. OBSまでの一連動作 | 最小限の変換プラグイン、書き出し形式、読み込みアプリ、ローカルOBS出力をつなぐ | haolanを書き出し、単体アプリで動かして自分のOBSに映せる |
| 4. 4090での品質確立 | 遮蔽・復帰、キャリブレーション、腕の補正、髪・服の揺れ、シェーダー表示を調整。OBS併用で遅延と継続動作を測る | 普段の会話・身振りで使える品質に達し、限界も分かる |
| 5. 配布できる形に整える | 非破壊変換、依存チェック、エラー表示、設定保存、導入手順を整備。対応範囲を明示する | 対応条件を満たすユーザーが開発者の手助けなしで導入できる |
| 6. 拡張を個別に追加 | 軽量設定、細かな指・表情、仮想カメラ、コラボ映像送信などを別々に追加する | 機能ごとに評価し、既存品質を保って追加できる |

フェーズ2で実際のアバター動作を確認してから、変換・配布の仕組みを拡充する。フェーズ3が最初の使える一式、4が日常利用したい品質、5が配布できる初期版。フェーズ6は一括で着手せず、必要な機能から進める。全身は撮影条件が変わるため独立した検証から始める。

後日の追加要件があるだけで初期版の完成条件を拡大しない。GPU動作の確認だけでフェーズ1を完了とせず、実映像での品質評価を残す。
