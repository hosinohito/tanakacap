# 瞼の入力確認（2026-09-21）

現在選択されているデモアバターは、左右のMMDキー「ウィンク２」「ウィンク２右」に閉眼入力が接続されている。部位別通信の人工入力（左1、右0）による既存Player画像 `results/partial-tracking/transport-basic.png` を確認し、片眼が閉じることを確認した。閉眼キーがない、または送信から反映まで一律に途切れている状態ではない。

現在選択されている録画 `20260911T235327-031115Z/camera.avi` の先頭900フレームを、現在の全部ON設定で再推論した。実写表示・実カメラ試験なし。0が開眼、1が閉眼。

| 最大閉眼値 | 左 | 右 |
|---|---:|---:|
| フィルター前 | 0.211 | 0.245 |
| 既存4フレーム法の後 | 0.079 | 0.165 |

入力の時点で閉じる値が小さく、フィルター後はさらに弱い。デモの瞼強調1.8倍でも完全閉眼には届かない。実写の閉眼正解を確認していないため、実際の瞬きをモデルが取り逃がしたか、撮影区間に閉眼が少なかったかは確定していない。別アバターの正常動作も保証しない。

現行推定は目の縦横比に固定値 `(0.27-ratio)/0.16` を適用する。改善候補は開眼/閉眼基準の調整と、短い瞬きを減衰させにくい瞼専用の時間処理。今回、推定・フィルター・アバター駆動は変更していない。頭などの4フレーム法を一括で変更しない。

再実行は `.\.venv\Scripts\python.exe tools/diagnostics/audit_eyelids.py`。保存済み動画だけを使用し、送信を停止した数値診断。出力は `results/eyelid-audit/`。`--video` と `--frames` で入力と検査範囲を指定できる。
