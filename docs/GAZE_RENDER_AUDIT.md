# 瞳の最終描画を比較（2026-09-12）

ユーザーは再度「目が動いているように見えない」と評価。顔は良好なので顔/目の別系統を維持し、モデルは変更していない。

最新211324の555frame中471frameで目線有効。送信yawは−11.03〜9.55、pitchは−6.85〜2.43。検出が全停止していた記録ではない。静止頭・顔へ目線のみ±20度を送ると、描画画像の瞳部分に差が生じることも確認した。ただし当時のPlayer受信状態/F4状態/実眼球の正解映像はないため、ユーザーが見た現象の全原因を断定しない。

前回の「約6px」は大きめの合成入力での頂点投影最大値だった。今回 tools/check_gaze_pixels.py で実PlayerのPNGに描かれたHAOLANの緑色虹彩を抽出し、左右眼それぞれの画素重心を比較した。頭・表情を固定、入力はyaw−10/+10、pitch−6/+6。これは実眼球の追跡精度を測るものではない。

| 表示感度 | 左右端間の移動（画面左眼/右眼） | 上下端間の移動 |
|---|---|---|
| 旧1倍 | 5.17 / 5.07 px | 3.32 / 3.14 px |
| 試行2倍 | 10.34 / 10.23 px | 5.75 / 5.69 px |

新しい既定感度は2倍。小さな検出を表示で強める試行であり、検出精度向上を意味しない。最大変形は横4mm/縦2.5mmのままで、最大可動域を広げていない。ロスト時の時定数0.5秒の正面復帰も不変。

tracking-settings.jsonのgaze_gainを1.0にすると前回感度、既定2.0（許容0.5〜3）。gaze_render_mode=bonesで旧骨方式、gaze_enabled=falseで推論OFF。Player/検証ツールの--gaze-gainにも対応。

検出側へ、左右別の虹彩ROI内座標・眼幅・正規化位置・採用後角度を数値ログとして追加。棄却したNaNはnullにし、JSON記録でキャプチャが停止しないことをテストした。画像は保存しない。送信角度と表示角度を区別し、Unity snapshotにも実際の適用角度を追加。

検証成果物はresults/gaze-render/pixel-check-1789161541640291200/report.jsonと10枚の固定入力画像。verified.logは実Bake方向/対象外不変/ロスト/F4相当OFF/既存部位の回帰成功。Python142件成功。最初の画素比較は検証ツールが掌付きfixtureしか許さず失敗し、顔だけのfixtureにも対応させた。感度増加後の復帰検査は3秒後の固定絶対値判定で失敗したため、意図する指数減衰の割合（99.7%以上）を検査する形に訂正。ランタイムの復帰速度は変更していない。

未確認：モデルがユーザーの意図した視線に正しく追従すること、顔/眼鏡/照明条件による偏り、ライブ表示での知覚上の改善。最終描画の停止は合成入力では再現せず、表示の小ささへの調整と次回切り分けの観測追加として扱う。
